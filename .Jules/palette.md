## 2024-08-10 - Semantic HTML for textarea labels
**Learning:** The application uses generic `<div>` elements for textarea labels instead of semantic `<label>` tags, leading to poor accessibility and missing click-to-focus functionality.
**Action:** Always prioritize semantic HTML (e.g., `<label for="...">`) for form elements to ensure proper accessibility and valid click-to-focus targets.
